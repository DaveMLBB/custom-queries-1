spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flight
    username: root
    password: ciao
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
  jackson:
    serialization:
      fail-on-empty-beans: false
